package users

import "testing"

func TestDatabase(t *testing.T) {
	/*err := Connect()
	assert.Nil(t, err)

	assert.True(t, uc.Exists())
	assert.True(t, bc.Exists())
	assert.True(t, ac.Exists())

	err = Disconnect()
	assert.Nil(t, err)*/
}
